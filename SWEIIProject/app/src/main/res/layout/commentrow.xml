<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">
    <ImageView
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:id="@+id/commentUserImage"
        android:src="@drawable/username_icon">
    </ImageView>
    <TextView
        android:layout_width="75dp"
        android:layout_height="20dp"
        android:layout_below="@+id/commentUserImage"
        android:layout_alignStart="@+id/commentUserImage"
        android:layout_marginTop="5dp"
        android:id="@+id/commentUsername"
        android:text="Username"
        android:textStyle="bold">
    </TextView>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignBottom="@+id/commentUsername"
        android:layout_alignTop="@+id/commentUserImage"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/commentUserImage"
        android:text="this is the users's comment on this post"
        android:textSize="20dp"
        android:id="@+id/userComment"/>
</RelativeLayout>
